# Deployment Directory

Optional deployment configuration for cloud platforms.

The main application uses:
- Backend: `backend/api/server.py`
- Frontend: `frontend/client/`

This directory contains optional deployment configs (e.g., Dockerfile for containerized deployment).

